# Export policy

Platform PNG files under `../social/` are the upload-ready format. Their paired
SVG files are editable masters. Template PNGs are previews until every bracketed
placeholder has been replaced. This folder holds delivery documentation rather
than duplicate binaries, keeping one authoritative export per platform path.
