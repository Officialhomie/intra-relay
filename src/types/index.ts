/**
 * Shared application types.
 *
 * Kept empty on purpose for the scaffold. Add cross-feature types here;
 * feature-local types belong in their own `src/features/<name>` folder.
 */

export type Route = "/" | "/request" | "/supplier/onboard" | "/operator" | "/docs";
