/**
 * Shared application types.
 *
 * Cross-feature types belong here; feature-local types stay in their own
 * `src/features/<name>` folder.
 */

export type Route = "/" | "/supplier/onboard" | "/operator" | "/docs";
